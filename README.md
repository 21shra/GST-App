# GST-App
APP :  *frontend/   ← TypeScript + React Native (Expo)  app/  ← Screens (index, scan, result, history, help)  src/   ← i18n + zustand store  app.json ← Expo config + Android perms  *backend/ ← Python + FastAPI ^server.py  ← All API routes + AI logic + validation requirements.txt BUILD_ANDROID.md  ← EAS build guide
